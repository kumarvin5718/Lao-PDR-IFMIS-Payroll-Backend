server:
  port: 8082

spring:
  application:
    name: payroll

  datasource:
    url: jdbc:postgresql://52.205.62.208/laopdr_dev_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: laopdr_dev_user
    password: laopdr@Dev754
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false
        jdbc.batch_size: 50
        order_inserts: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect