apiVersion: v1
kind: Service
metadata:
  name: ifms-payroll-api
  labels:
    app.kubernetes.io/name: ifms-payroll
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ifms-payroll
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 8000
      targetPort: http
